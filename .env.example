# ============================================
# REQUIRED - These MUST be configured
# ============================================

# Database - Replace with your production database URL
DATABASE_URL="postgresql://postgres:CHANGE_THIS_PASSWORD@localhost:5432/timeless_luxury?schema=public"

# NextAuth - CRITICAL: Generate a secure random string (min 32 characters)
# Generate with: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="GENERATE_WITH_openssl_rand_base64_32_MINIMUM_32_CHARS_REQUIRED"

# ============================================
# Docker Compose Variables (if using Docker)
# ============================================

POSTGRES_USER="postgres"
POSTGRES_PASSWORD="CHANGE_THIS_PASSWORD"
POSTGRES_DB="timeless_luxury"

# ============================================
# Application Settings
# ============================================

NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
